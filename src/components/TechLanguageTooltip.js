const TechLanguageTooltip = (props) => {
    return (
        <div className="TechLanguageTooltip" style={{
        }}>
            <p>{props.tooltip}</p>
        </div>
    );
}

export default TechLanguageTooltip;